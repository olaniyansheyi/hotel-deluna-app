import styled from "styled-components";

export const Button = styled.button`
  font-size: 1.4rem;
  padding: 1.3rem 1.6rem;
  font-weight: 500;
  border: none;
  border-radius: var(--border-radius-sm);
  background-color: purple;
  color: var(--brand-sm-50);
  box-shadow: var(--shadow-sm);
  cursor: pointer;
`;
